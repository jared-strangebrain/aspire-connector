{
  "name": "contacts.create",
  "title": "contacts.create",
  "description": "contacts.create operation",
  "version": "1.0.0",
  "input": { "schema": { "type": "object", "properties": {} } },
  "output": { "schema": { "type": "object", "properties": {} } }
}
